import { useSelector } from 'react-redux'
// import {saveUserInfo} from '../reduxStore/userSlice'
// import {selectCurrentUser} from '../reduxStore/apiSlice'

const Dashboard = () => {
    // const user = useSelector((state) => saveUserInfo(state))
    // console.log(user)
    // const user = useSelector(selectCurrentUser(state))
    // console.log(user)
    return(
        <>
        <p>Dashboard page</p>
        </>
    )
}

export default Dashboard